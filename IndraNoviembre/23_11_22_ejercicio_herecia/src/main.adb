with rectangulo_model; use rectangulo_model;
with figura; use figura;


procedure Main is

   Rectangulo1 : Rectangulo := Create("Primera figura");

begin
   --  Crear una clase abstracta figura (sin atributos)
   -- Crear el metodo abstracto calcular area
   --Crear la subclase rectangulo(Base,Altura) que hereda de figura
   --Sobreescribir (Overriding) el metodo clacular_are
   null;
end Main;
